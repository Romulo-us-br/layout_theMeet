{
  "name": "layout_themett",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
 "scripts": {
  "copy-img": "shx rm -rf dist/image && shx mkdir -p dist/image && shx cp -r src/image/. dist/image/",
  "build": "npm run copy-img && sass src/scss/main.scss dist/css/main.css --style compressed",
  "deploy": "npm run build && npx gh-pages -d dist --no-history"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "sass": "^1.97.3",
    "shx": "^0.4.0"
  }
}